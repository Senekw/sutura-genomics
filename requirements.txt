# ARCA — graph-based deep learning for non-rigid tissue alignment
# Install with:  pip install -r requirements.txt
#
# The line below points pip at PyTorch's CPU wheel index so `torch` resolves
# to the CPU build (no CUDA) on Windows.
--extra-index-url https://download.pytorch.org/whl/cpu

# --- Core scientific stack ---
numpy>=1.24,<2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7

# --- Single-cell / spatial transcriptomics ---
anndata>=0.10
scanpy>=1.10
squidpy>=1.4
datashader>=0.16  # transitive dep of spatialdata/squidpy not auto-installed

# --- Optimal transport baselines ---
POT>=0.9
paste-bio>=1.4

# --- Deep learning (CPU) ---
torch>=2.2
torch_geometric>=2.5
