# Pinned dependency set for the sutura-align container image.
# Versions are the exact ones the module was validated against (see README "Provenance").
# torch resolves to the CPU wheel via the extra index passed on the pip command line
# (see Dockerfile); no CUDA is required for the alignment engine.
#
# --- core scientific stack ---
numpy==1.26.4
scipy==1.17.1
scikit-learn==1.9.0
pandas==2.3.3
h5py==3.16.0
natsort==8.4.0
networkx==3.6.1
numba==0.65.1
llvmlite==0.47.0
pynndescent==0.6.0

# --- single-cell / spatial IO (scanpy provides read_visium for Space Ranger dirs) ---
anndata==0.12.17
scanpy==1.11.5

# --- optimal-transport baseline (PASTE2 partial alignment) ---
POT==0.9.6.post1
paste2==1.0.1

# --- graph neural network (CPU; native scatter, no compiled extensions) ---
torch==2.12.1
torch-geometric==2.8.0
