"""Sutura CLI core: UI-independent agent loop, tools, and bundle writer.

Nothing in this package imports the TUI. Everything here is exercised headlessly
by the end-to-end test, so the full workflow can run without a terminal.
"""
