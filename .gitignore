# Python env
.venv/
__pycache__/
*.pyc

# Large raw data — regenerable via src/prepare_data.py and src/warp_slice.py
data/
research/data/
*.h5ad

# Large transport matrices — regenerable via the sweep scripts (exceed GitHub's
# 100 MB limit at full-spot resolution). Results CSVs, PNG figures, .pt
# checkpoints, and .npz metadata ARE tracked (they are the deliverables).
*.npy

# Notebook checkpoints
.ipynb_checkpoints/

# Transient run logs at repo root (results/*.log are intentionally tracked)
/err.log
/sweep_run.log
/train_sb.log
/train_sb_strict.log
/train_bc.log
/autoadapt.log
/server.log

# Backend runtime job storage / uploads / outputs (regenerated per request)
backend/_jobs/
backend/_e2e_download.h5ad
/genmax.log
